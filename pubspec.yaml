name: favelasemcorona
description: Ajudando a diminuir o impacto do virus nas comunidades

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  google_maps_flutter: ^0.5.25+1
  google_maps_webservice: ^0.0.13
  http: ^0.12.0+1
  json_serializable: ^2.0.2
  cloud_firestore: ^0.9.13+1
  rflutter_alert: ^1.0.3
  firebase_core: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.4.0
  build_runner: ^1.2.7

flutter:
  uses-material-design: true

  assets:
  - images/
